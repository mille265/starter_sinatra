<!DOCTYPE html>
<html>
<head>
    <title>New River Form</title>
</head>
<body>

    <h1>Create a new river</h1>

    <form action="/rivers" method="POST">

        <label for="name_river">Name of River</label>
        <input type="text" name="name_river">

        <label for="name_continent">Continent</label>

        <select name="name_continent">
            <% Continent.all.each do |continent| %>
              <option value="<%= continent.id %>"><%= continent.name %></option>
            <% end %>
        </select>
        
        <label for="num_countries_pass_through">  Number of Countries River Passes Through</label>
        <input type="text" name="num_countries_pass_through">
        <input type="submit" value="Create River">
    </form>

</body>
</html>