<!DOCTYPE html>
<html>
<head>
    <title>Edit River Form</title>
</head>
<body>

    <h1>Edit This River</h1>


    <form action="/rivers/<%=@river.id%>" method="POST">
        <input type="hidden" name="_method" value="PATCH">
        <label for="continent_name">Continent</label>
        <select name="continent_name">
            <% Continent.all.each do |continent| %>
             <option <%if continent.id == @river.continent.id%> 
                selected <%end%>
              value="<%= continent.id %>"><%= continent.name %></option>
            <% end %>
         </select>
        <label for="river_name">Name of River</label>
        <input type="text" name="river_name" value="<%=@river.name%>">
        <label for="num_countries_pass_through">Number of Countries River Passes Through</label>
        <input type="text" name="num_countries_pass_through" value=<%=@river.num_countries_pass_through%>>
        <div>
        <input type="submit" value="Update River">
        </div>
    </form>

    <form action="/rivers/<%=@river.id%>" method='redirect'>
        <input type="hidden" name="_method" value="patch">
        <input type="submit" value="Cancel Edit">
    </form>



</body>
</html>
